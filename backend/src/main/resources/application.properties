spring.application.name=backend
spring.datasource.url=jdbc:postgresql://localhost:5432/ecommerce_db
spring.datasource.username=postgres
spring.datasource.password=2406
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Serve product images from a folder on disk (mapped to GET /pictures/**).
# Default here assumes backend working dir is ".../Project/backend" and images are in ".../Project/Picture".
app.images.dir=../Picture
